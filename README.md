# Toolbox


```
hostmux host_a host_b host_c      Create a tmux session and connect to the specified hosts in separate panes/splits
                                  Use synchronize-panes to multiplex commands
```
